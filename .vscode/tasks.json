{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build with colcon",
            "type": "shell",
            "command": "cd ~/ws && colcon build --symlink-install --packages-skip px4_msgs microxrcedds_agent",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}